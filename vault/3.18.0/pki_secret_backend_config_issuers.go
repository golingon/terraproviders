// CODE GENERATED BY github.com/volvo-cars/lingon. DO NOT EDIT.

package vault

import (
	"encoding/json"
	"fmt"
	"github.com/volvo-cars/lingon/pkg/terra"
	"io"
)

// NewPkiSecretBackendConfigIssuers creates a new instance of [PkiSecretBackendConfigIssuers].
func NewPkiSecretBackendConfigIssuers(name string, args PkiSecretBackendConfigIssuersArgs) *PkiSecretBackendConfigIssuers {
	return &PkiSecretBackendConfigIssuers{
		Args: args,
		Name: name,
	}
}

var _ terra.Resource = (*PkiSecretBackendConfigIssuers)(nil)

// PkiSecretBackendConfigIssuers represents the Terraform resource vault_pki_secret_backend_config_issuers.
type PkiSecretBackendConfigIssuers struct {
	Name      string
	Args      PkiSecretBackendConfigIssuersArgs
	state     *pkiSecretBackendConfigIssuersState
	DependsOn terra.Dependencies
	Lifecycle *terra.Lifecycle
}

// Type returns the Terraform object type for [PkiSecretBackendConfigIssuers].
func (psbci *PkiSecretBackendConfigIssuers) Type() string {
	return "vault_pki_secret_backend_config_issuers"
}

// LocalName returns the local name for [PkiSecretBackendConfigIssuers].
func (psbci *PkiSecretBackendConfigIssuers) LocalName() string {
	return psbci.Name
}

// Configuration returns the configuration (args) for [PkiSecretBackendConfigIssuers].
func (psbci *PkiSecretBackendConfigIssuers) Configuration() interface{} {
	return psbci.Args
}

// DependOn is used for other resources to depend on [PkiSecretBackendConfigIssuers].
func (psbci *PkiSecretBackendConfigIssuers) DependOn() terra.Reference {
	return terra.ReferenceResource(psbci)
}

// Dependencies returns the list of resources [PkiSecretBackendConfigIssuers] depends_on.
func (psbci *PkiSecretBackendConfigIssuers) Dependencies() terra.Dependencies {
	return psbci.DependsOn
}

// LifecycleManagement returns the lifecycle block for [PkiSecretBackendConfigIssuers].
func (psbci *PkiSecretBackendConfigIssuers) LifecycleManagement() *terra.Lifecycle {
	return psbci.Lifecycle
}

// Attributes returns the attributes for [PkiSecretBackendConfigIssuers].
func (psbci *PkiSecretBackendConfigIssuers) Attributes() pkiSecretBackendConfigIssuersAttributes {
	return pkiSecretBackendConfigIssuersAttributes{ref: terra.ReferenceResource(psbci)}
}

// ImportState imports the given attribute values into [PkiSecretBackendConfigIssuers]'s state.
func (psbci *PkiSecretBackendConfigIssuers) ImportState(av io.Reader) error {
	psbci.state = &pkiSecretBackendConfigIssuersState{}
	if err := json.NewDecoder(av).Decode(psbci.state); err != nil {
		return fmt.Errorf("decoding state into resource %s.%s: %w", psbci.Type(), psbci.LocalName(), err)
	}
	return nil
}

// State returns the state and a bool indicating if [PkiSecretBackendConfigIssuers] has state.
func (psbci *PkiSecretBackendConfigIssuers) State() (*pkiSecretBackendConfigIssuersState, bool) {
	return psbci.state, psbci.state != nil
}

// StateMust returns the state for [PkiSecretBackendConfigIssuers]. Panics if the state is nil.
func (psbci *PkiSecretBackendConfigIssuers) StateMust() *pkiSecretBackendConfigIssuersState {
	if psbci.state == nil {
		panic(fmt.Sprintf("state is nil for resource %s.%s", psbci.Type(), psbci.LocalName()))
	}
	return psbci.state
}

// PkiSecretBackendConfigIssuersArgs contains the configurations for vault_pki_secret_backend_config_issuers.
type PkiSecretBackendConfigIssuersArgs struct {
	// Backend: string, required
	Backend terra.StringValue `hcl:"backend,attr" validate:"required"`
	// Default: string, optional
	Default terra.StringValue `hcl:"default,attr"`
	// DefaultFollowsLatestIssuer: bool, optional
	DefaultFollowsLatestIssuer terra.BoolValue `hcl:"default_follows_latest_issuer,attr"`
	// Id: string, optional
	Id terra.StringValue `hcl:"id,attr"`
	// Namespace: string, optional
	Namespace terra.StringValue `hcl:"namespace,attr"`
}
type pkiSecretBackendConfigIssuersAttributes struct {
	ref terra.Reference
}

// Backend returns a reference to field backend of vault_pki_secret_backend_config_issuers.
func (psbci pkiSecretBackendConfigIssuersAttributes) Backend() terra.StringValue {
	return terra.ReferenceAsString(psbci.ref.Append("backend"))
}

// Default returns a reference to field default of vault_pki_secret_backend_config_issuers.
func (psbci pkiSecretBackendConfigIssuersAttributes) Default() terra.StringValue {
	return terra.ReferenceAsString(psbci.ref.Append("default"))
}

// DefaultFollowsLatestIssuer returns a reference to field default_follows_latest_issuer of vault_pki_secret_backend_config_issuers.
func (psbci pkiSecretBackendConfigIssuersAttributes) DefaultFollowsLatestIssuer() terra.BoolValue {
	return terra.ReferenceAsBool(psbci.ref.Append("default_follows_latest_issuer"))
}

// Id returns a reference to field id of vault_pki_secret_backend_config_issuers.
func (psbci pkiSecretBackendConfigIssuersAttributes) Id() terra.StringValue {
	return terra.ReferenceAsString(psbci.ref.Append("id"))
}

// Namespace returns a reference to field namespace of vault_pki_secret_backend_config_issuers.
func (psbci pkiSecretBackendConfigIssuersAttributes) Namespace() terra.StringValue {
	return terra.ReferenceAsString(psbci.ref.Append("namespace"))
}

type pkiSecretBackendConfigIssuersState struct {
	Backend                    string `json:"backend"`
	Default                    string `json:"default"`
	DefaultFollowsLatestIssuer bool   `json:"default_follows_latest_issuer"`
	Id                         string `json:"id"`
	Namespace                  string `json:"namespace"`
}
